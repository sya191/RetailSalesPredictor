To run the front-end web app:
1. Install dependencies:  pip install -r requirements.txt
2. Train model (if needed): python train_and_evaluate.py
3. Start server: python app.py
4. Open browser: http://localhost:5000/
